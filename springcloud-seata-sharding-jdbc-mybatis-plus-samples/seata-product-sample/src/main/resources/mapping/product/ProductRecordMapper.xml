<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.seata.product.mapper.ProductRecordMapper">
    <insert id="insertRecord">
        INSERT INTO product_record (id, product_id, product_name, buy_num)
        VALUES (#{id}, #{productId}, #{productName}, #{buyNum})
    </insert>
</mapper>
